spring:
  application:
    name: restaurentservice

  datasource:
    url: jdbc:mysql://localhost:3306/restaurentservice
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  cloud:
    config:
      enabled: false